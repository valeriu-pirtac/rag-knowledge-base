"""Infrastructure layer - External service implementations.

This layer implements domain protocols using external services and frameworks.
It depends on the domain layer (via protocols) and application layer.
"""
